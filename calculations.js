export function add(x, y) {
const sum = x + y;
return sum;
};
export function subtract(x, y) {
    const subtract = x - y; 
    return subtract;
};
export function multiply(x, y) {
    const multiply = x * y; 
    return multiply;
}
